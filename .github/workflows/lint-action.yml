name: Actionlint CI
on: 
  push:
    branches:
      - feat/addActionsLinter
    paths:
      - '.github/**.yml'
  workflow_dispatch:
jobs:
  exec:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run ActionLint
        shell: bash
        run: |
          bash <(curl https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)
          ./actionlint -color -ignore 'SC2002'